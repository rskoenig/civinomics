#
# Pylowiki - Pylons configuration
#
# The %(here)s variable will be replaced with the parent directory of this file
#
[DEFAULT]
debug = true
email_to = todda@greenocracy.org
smtp_server = localhost
error_email_from = todda@greenocracy.org

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 2224

[app:main]
use = egg:Pylowiki
full_stack = true
static_files = true

cache_dir = %(here)s/data
beaker.session.key = pylowiki
beaker.session.secret = H5fdm4wpOfQVWt5BdzbE0DW8T
beaker.session.cookie_domain = localhost
app_instance_uuid = {0c3fd561-5507-4914-a99c-b1a7db38c690}

# If you'd like to fine-tune the individual locations of the cache data dirs
# for the Cache data, or the Session saves, un-comment the desired settings
# here:
#beaker.cache.data_dir = %(here)s/data/cache
#beaker.session.data_dir = %(here)s/data/sessions

# SQLAlchemy database URL
sqlalchemy.url = mysql://root:Gr33n0(r4(y@Localhost/todda
sqlalchemy.pool_recycle = 3600

# WARNING: *THE LINE BELOW MUST BE UNCOMMENTED ON A PRODUCTION ENVIRONMENT*
# Debug mode will enable the interactive debugging tool, allowing ANYONE to
# execute malicious code after an exception is raised.
set debug = false

#
# Pylowiki configutaion
#

# salt used to hash passwords, Be sure to change this string
auth.pass.salt = pylowiki

# browser titlebar string (typically the domain for the site)
custom.titlebar = greenocracy.org

# Optionally create an admin account
# The admin config options are sourced when running "paster setup-app config.ini"

admin.firstName  = Toddoichi
admin.lastName = Anderson
admin.pass  = westside
admin.email = todda@greenocracy.org
admin.zipCode = 95060

admin2.firstName = manu
admin2.lastName = koenig
admin2.pass = ha, youw ish
admin2.email = manu@greenocracy.org
admin2.zipCode = 95060

admin3.firstName = robert
admin3.lastName = singleton
admin3.pass = ha, you wish
admin3.email = robert@greenocracy.org
admin3.zipCode = 95060

admin4.firstName = demo
admin4.lastName = demo
admin4.pass = slingBling5
admin4.email = demo@greenocracy.org
admin4.zipCode = 95060

# please insert the contact us email address
contact.email = todda@greenocracy.org

# email from registering
activation.email = todda@greenocracy.org

# upload.path, path upload attachmentss
upload.path = %(here)s/pylowiki/public/attachment

# public.reg, may the public register?
public.reg = true

# public.sitemap, may the public view the sitemap?
public.sitemap = true

# public.search, may the public use the search?
public.search = true

# allow.comments, are users allow to comment on pages?
allow.comments = true

# Optional Google analytics key (insert key only)
google.analytics =

# Optional, create a dir under public named after theme, we will load style.css  
#load.theme = chaostheroy 
load.theme =

# Directory for storing user submitted pictures
avatarDirectory = %(here)s/pylowiki/public/images/avatars
slideshowDirectory = %(here)s/pylowiki/public/images/slideshows
govtSphereDirectory = %(here)s/pylowiki/public/images/govtSpheres
articleDirectory = %(here)s/pylowiki/public/images/news

[app:php-app]
use = egg:wphp
base_dir = %(here)s/php
option magic_quote_gpc = Off

# Logging configuration
[loggers]
keys = root

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s] %(message)s
